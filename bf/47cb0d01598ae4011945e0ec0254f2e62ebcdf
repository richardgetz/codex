intent:
  id: "intent/2026-06-18/stable-rust-v0.140.0#bf47cb0d0"
  change_type: "chore"
  scope: "stable-release"
  summary: "Merge upstream rust-v0.140.0 into the fork stable branch."
  decision: "Use a stable-based merge of upstream rust-v0.140.0 and resolve conflicts by preserving fork-only account, scratchpad, memory, MCP visibility/recovery, app-server, TUI, git intent note, conventional commit, enablement, and CI/release surfaces while adopting upstream 0.140.0 release changes."
  constraints:
    - "Must preserve fork-only runtime features and slash commands, including account aliases, scratchpad/continuous policy, memory maintenance, agent pruning, and MCP recovery behavior."
    - "Must keep generated config schema, app-server schemas, Cargo lock, and Bazel module lock artifacts aligned with resolved sources."
    - "Must keep state migrations unique; incoming upstream migrations are renumbered after existing fork migrations."
    - "Must preserve app-server/TUI realtime routing and selected executor skill precedence over host skill name collisions."
    - "Must not touch sandbox network environment variable behavior."
  intent_priority: "must"
  code_locations:
    - "codex-rs/core"
    - "codex-rs/codex-mcp"
    - "codex-rs/core-skills"
    - "codex-rs/ext/skills"
    - "codex-rs/app-server"
    - "codex-rs/app-server-protocol"
    - "codex-rs/tui"
    - "codex-rs/state/migrations"
    - "codex-rs/tools"
    - "sdk/python"
    - ".github/workflows"
  requested_by: "@rick"
  recorded_by:
    agent: "codex-cli"
    model: "gpt-5"
