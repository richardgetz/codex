load("//:defs.bzl", "codex_rust_crate")

codex_rust_crate(
    name = "code-mode",
    crate_name = "codex_code_mode",
    rustc_env = {
        # Keep manifest-root path lookups inside the Bazel execroot for local-build discovery.
        "CARGO_MANIFEST_DIR": "codex-rs/code-mode",
    },
)
