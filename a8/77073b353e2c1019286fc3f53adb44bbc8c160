intent:
  id: "intent/2026-05-22/stable-rust-v0.133.0#a877073b353e"
  change_type: "chore"
  scope: "stable-release"
  summary: "Merge upstream rust-v0.133.0 into the fork stable branch."
  decision: "Use a stable-based merge of upstream rust-v0.133.0 and resolve conflicts by preserving fork-only account, memory, scratchpad, session control, app-server, packaging, state migration, SDK/schema, and gh Keychain sandbox surfaces while adopting upstream 0.133.0 release changes."
  constraints:
    - "Must preserve fork-only runtime features, slash commands, and app-server policy controls."
    - "Must keep gh credential helpers able to reach macOS SecurityServer from workspace-write Seatbelt sandboxes."
    - "Must keep generated config, app-server schema, Python SDK, Bazel, and Cargo lock artifacts aligned with resolved sources."
    - "Must preserve shipped state migration checksums and backfill legacy thread goals into the split goals database before dropping the legacy table."
    - "Must keep fork npm packaging identity, codex-rick executable behavior, arm64 fork release lane, and trusted-publishing safety intact."
    - "Must not touch sandbox network environment variable behavior."
  intent_priority: "must"
  code_locations:
    - "codex-rs/sandboxing/src/seatbelt_base_policy.sbpl"
    - "codex-rs/sandboxing/src/seatbelt_tests.rs"
    - "codex-cli/scripts/build_npm_package.py"
    - "codex-cli/scripts/install_native_deps.py"
    - "scripts/stage_npm_packages.py"
    - ".github/workflows/fork-release.yml"
    - "codex-rs/state/src/runtime.rs"
    - "codex-rs/state/migrations"
    - "codex-rs/app-server-protocol"
    - "sdk/python"
  requested_by: "@rick"
  recorded_by:
    agent: "codex-cli"
    model: "gpt-5"
