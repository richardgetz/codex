Intent: reduce memory overhead in the optional Obscura packaging path by streaming SHA-256 verification instead of loading each 35-52 MB release archive into memory. This keeps the built-in browser resource hydration path leaner without changing the pinned asset trust model.\n\nValidation: Python 3.13 py_compile for install_native_deps.py; install_native_deps.py --help exposes the obscura component; real smoke fetch for --component obscura --target aarch64-apple-darwin installed Obscura using the streaming verifier; git diff --check.
